import { useState, useEffect, forwardRef } from 'react'


type Props = {
    onSearch: () => void
}
const Navbar  = ({ onSearch }:Props) => {
    return (
        <div>Navbar</div>
    )
}

export default Navbar